services:
  backend:
    container_name: zorzon_backend
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    platform: linux/arm64 #for platform error on Apple M1 chips
    ports:
      - "1338:1338" # After rev proxy caddy do port exposed for internal communication
    env_file:
      - ./backend/.env
    environment:
      DATABASE_HOST: zorzon_db
      NODE_ENV: production # Ensure it's running in production mode
    depends_on:
      - db
    networks:
      - zorzon

  frontend:
    container_name: zorzon_frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    platform: linux/arm64 # Ensuring compatibility with ARM-based systems
    volumes:
      - ./frontend:/app
      - ./frontend/.env:/opt/app/.env
    ports:
      - "5174:5174"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - zorzon
    depends_on:
      - backend

  db:
    container_name: zorzon_db
    image: postgres:15
    platform: linux/arm64 #for platform error on Apple M1 chips
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backup:/docker-entrypoint-initdb.d # Mount backup directory to init scripts
    environment:
      POSTGRES_USER: strapi
      POSTGRES_PASSWORD: strapi
      POSTGRES_DB: strapi
    ports:
      - "5433:5432"
    networks:
      - zorzon

volumes:
  postgres_data:

networks:
  zorzon:
    driver: bridge
